# Input:
Let focus on test the user interface by creating tasks 

# Output
To test the User Interface (UI) effectively, we need to verify that our `Rich`-powered CLI provides clear feedback and correctly visualizes the hierarchy. As your Senior Engineer, I've prepared a **Test Script Execution Plan** that you can run right now to see the system in action.

### 🧪 UI Test Suite: "The Nested Project"

This test sequence verifies three things:
1.  **Creation Feedback:** Do we get clear UUID and success confirmation?
2.  **Visual Hierarchy:** Does the `tree` command correctly indent children?
3.  **State Reflection:** Does the `list` command show the "Materialized View" accurately?

---

### Phase 1: Creating the Hierarchy
Run these commands in your terminal. 

> **Note:** For the second command, copy the ID generated by the first.

```bash
# 1. Create a Parent Task
uv run task-traveler create "Develop Login API" --desc "Backend authentication logic"

# 2. Create a Child Task (Replace <ID> with the ID from above)
uv run task-traveler create "Write JWT Logic" --parent <ID>

# 3. Create another Child
uv run task-traveler create "Setup Redis Cache" --parent <ID>
```

---

### Phase 2: Testing UI Visualization
Now, we verify how the "Product Owner" sees the data.

#### The `list` Command (Flat View)
This test ensures we can see all tasks at a glance with their truncated IDs.
```bash
uv run task-traveler list
```
**Expected UI Output:**
A `Rich` table showing 3 tasks. The "Parent ID" column should show the link for the two child tasks.

#### The `tree` Command (Hierarchical View)
This is the "Senior Engineer" specialty—ensuring the mental model of the user matches the UI.
```bash
uv run task-traveler tree
```
**Expected UI Output:**

```text
Develop Login API [todo] (a1b2c3)
 ├── Write JWT Logic [todo] (d4e5f6)
 └── Setup Redis Cache [todo] (g7h8i9)
```

---

### Phase 3: Testing Real-time Updates
Let's verify that updating a child doesn't break the parent's UI state.

```bash
# Update a child status
uv run task-traveler update <CHILD_ID> --status in_progress

# View the tree again
uv run task-traveler tree
```
**Verification:** Does the tree now show `[in_progress]` for the child while keeping the parent as `[todo]`?

---

### 🔍 UI Edge-Case Checklist
When you run these, look for the following "polished" UI details I've implemented:
* **Color Coding:** `todo` should be white/blue, `in_progress` should be yellow, and `done` should be green.
* **Error Handling:** Try running `update` with a fake ID. Does it give a "Senior" error message or a raw Python Traceback? (It should be a clean Red Error).
* **ID Truncation:** We use full UUIDs internally, but the UI should show only the first 8 characters for readability.

### 🚀 Next Step
Once you've confirmed the UI looks good, would you like to perform the **"The Big Undo"** test? We will create a task, undo it, and verify the UI updates to show a "blank" state while the database keeps the history.